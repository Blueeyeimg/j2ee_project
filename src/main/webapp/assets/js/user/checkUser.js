function checkUser(){
    var result = document.getElementById("form-username").value;
    var password = document.getElementById("form-password").value;
    if(result == ""  ){
        alert("用户名不能为空");
        return false;
    }
    if(password == ""  ){
        alert("密码不能为空");
        return false;
    }
}
var user = .getAttribute("user");
window.alert(user);
document.getElementById(ee).innerHTML(user.password);